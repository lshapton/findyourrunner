<script>

$.ajax({
  url: '/runners/1',
  type: "put",
  dataType: "script",
  data: { 
    latitude: "<%= Runner.find(1).latitude + 1 %>",
    longitude: "<%= Runner.find(1).longitude + 1 %>",
    _method: 'put'
  }
});

</script>

