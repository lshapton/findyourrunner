<script>

$.ajax({
  url: '/runners/1',
  type: "put",
  dataType: "script",
  data: { 
    latitude: "<%= Runner.find(1).latitude %>",
    longitude: "<%= Runner.find(1).longitude %>",
    _method: 'put'
  }
});

</script>

